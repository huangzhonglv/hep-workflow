# Run Instructions

## Metadata

- Generated at: `{{GENERATED_AT}}`
- Result directory: `{{RESULT_DIR}}`
- Runnable code: `{{RESULT_WL_PATH}}`

## Command

```bash
{{RUN_COMMAND}}
```

## Expected Outputs

{{EXPECTED_OUTPUTS}}

## Notes

- If you run inside the Codex sandbox on this machine, `wolframscript` may report a misleading license error.
- Prefer running the command outside the sandbox for real verification.
- If your Wolfram installation lives elsewhere, replace `/Applications/Wolfram.app/Contents/MacOS/wolframscript` with your local path.
